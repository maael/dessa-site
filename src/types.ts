export interface LinkData {
  context: {
    mount_index: number
    map_id: number
    player_x: number
    player_y: number
  }
  identity: {
    name: string
    race: number
    profession: number
    spec: number
  }
}
